# Rodada 99 · Executora · progresso

- **23:23** · rodada aberta. `git pull --rebase` recusado pelo `lore/` sujo do Cartógrafo; `git fetch`: `HEAD` = `origin/main` = `118402b` (o despacho). Despacho lido inteiro.
- **23:23-23:25** · item 1, `git log -S` na frase de cada um: **nenhum dos três foi consertado e desfeito.** "Tick 0" em `relacoes-sociais.md` só aparece no commit que o criou (`738aaee`, 20/07); `{MOLDES.aura}` em `artes/regras.astro` só no que o criou (`86fb96d`, 18/08), e `MOLDES.aura.nota` nunca existiu; "Esp." em `qual-sistema.md`/`defesas.md` só nos que os criaram (`19b3b2f`, `f86358a`), e nenhuma legenda de "Esp." entrou e saiu. Os três seguem no fonte (relidos agora). Foi marca errada, não conserto desfeito.
- **23:23-23:25** · C-22 escrito: a frase passa a Tick 1 + degrau de 6 pontos, com link para a iniciativa física (`regras.json` `derivados.iniciativa`: `tickDoPrimeiro` 1, `gapPorPenalidade` 6). Não citei o contrapé: a frase velha não o citava e dizer que ele vale no social seria regra que o despacho não pede.
- **23:25** · (correção de registro: as duas linhas acima tinham horas escritas sem ler a máquina; a faixa agora é entre as duas leituras de `date`, 23:23 e 23:25.) C-39: `{MOLDES.aura}` passa a `{MOLDES.aura.nota}`. C-61: legenda "Esp. é a Especialidade" com link, na linha da folha de referência de `defesas.md` (só essa linha) e numa frase logo abaixo do fluxograma de `qual-sistema.md`. C-23: o dado e o motor concordam (`calc.ts` `valorPassivo` = (Atributo + Habilidade) × 2 + Centelha; o glossário diz o mesmo, com a Especialidade por cima só na situação); a célula da tabela dos modos em `acoes-e-sistema.md` passa à fórmula com a Centelha, e a mesma omissão em `acoes-sentidos-e-engano.md` ("2 × (Percepção + Prontidão)") também. C-102: a contagem sai do `monsters.json` no frontmatter; hoje dá **101 das 309** (o item dizia 100, e já envelheceu de novo desde 18/09). C-103: +2d6 e +3d6, como a tabela de `combate.md`.
- **23:28** · C-104: três linhas de comentário logo acima do `const DATA` de `conversao-monstros.html` (conferido de novo: 88 notas citam "Centelha N", 57 com N = cent − 1). Parciais, só medidos: **C-12** faltam as cinco linhas do Bram (Atributos, Habilidades, Secundárias, Especialidades, Virtudes; `cost-examples.mjs` diz "5 linha(s) divergem") e a de Técnicas não conferível (A-03); é **decisão**, porque a M-02 registrou deixá-las como escolha consciente, e duas delas (Secundárias, Técnicas) nem são deriváveis do que o capítulo publica. **C-47** faltam os três números da M-07 (armadura tira dado da Furtividade, Esquiva encurralada −2 a −6, segunda Firula desce um nível); decididos em 16/09, é **execução**. **C-85** falta o link do ☆ em `/artes/catalogo` e em `/caminhos/[id]` (as duas com 0 menções a `marcadores`); é **execução**, uma frase por página.
- **23:34** · `bed1e91` empurrado (o `ef4271c` reaplicado por cima do `8244e9e` da Revisora, que chegou no meio; o `pull --rebase` é recusado pelo `lore/` sujo, então foi `reset --keep` + `cherry-pick`, que não encosta no `lore/`). Marcas nos dois arquivos de consertos, `Pendencias.md` §2 e relato escritos.
