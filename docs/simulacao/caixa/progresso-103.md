# Rodada 103 · Executora · progresso

- **03:56** · rodada aberta: `status` vazio, `fetch`, `merge-base --is-ancestor` verdadeiro, `switch -C executora origin/main` em `39a0de9`. Despacho lido. Antes, avisei o Arquiteto do CI da 102 (`test-luas` caiu no lançamento do Chrome).
- **03:58** (as duas linhas foram escritas juntas, na mesma leitura de `date`) · o conserto: função nova `faixaNaFolha(arma, metros, classe)` em `src/lib/alcance.ts`, que devolve `null` para a classe `arremesso` e passa o resto para `faixaDeDistancia`; a folha (`grid.astro`, bloco "O ALCANCE") passa a chamá-la com a classe do ataque. `alcanceDaArma`, `faixaDeDistancia` e `alcanceInterpor` não mudaram (só o comentário de `alcanceDaArma`, que mentia). Teste novo `scripts/test-folha-arremesso.mjs`, no `npm run validate` logo depois do `test-combate-tempo`.
- **03:58** · os três sentidos do teste, lidos: com o conserto, verde (16 ✓, exit 0); contra o código de hoje (`git stash push -- src/lib/alcance.ts src/pages/mesa/grid.astro`, só os meus dois), **vermelho, 3 ✘, exit 1** ("existe a função que a folha chama", e as duas asserções da tela); com o conserto desfeito só na função (a linha do `arremesso` tirada), **vermelho, 8 ✘** (as 8 armas); com o conserto desfeito só na tela (a folha de volta a `faixaDeDistancia`), **vermelho, 2 ✘**. Restaurado, verde de novo.
- **04:02** · medição na tela refeita, antes (`git stash` dos meus dois arquivos) e depois, com a sonda da `medicao-i12.md` estendida (besta antes da troca; um tabuleiro de 90 colunas para alvos a 15 e 30 m). Antes: adaga e azagaia com faixa a 3, 4 e 7 m, e 'Além do alcance máximo (10 m)' para a adaga a 15 e 30 m. Depois: calada em todas. A besta: calada dentro do livre nas duas, e **a 54 m (bench 30, 120 colunas) mostra '1ª faixa (livre até 40 m, máximo 100 m): -3'** depois do conserto. Rodando validate e build.
- **04:03** · `validate` vermelho da primeira vez: 7 citações a `alcance.ts` envelheceram com as linhas novas (`L-simulacao-simultaneo.md`). `node scripts/reapontar.mjs` reapontou 36 por mapa do diff (L-simulacao-simultaneo, ESTADO, VOZ e CONTEXTO, que estavam limpos antes) e avisou 4 ambíguas em `grid.astro`; o portão não acusa nenhuma delas. `validate` de novo: exit 0. `build`: exit 0. A `medicao-i12.md` ganhou a seção da rodada 103 com a tabela de antes e depois.
- **04:04** · travessão contado arquivo a arquivo contra o `HEAD`: nenhum aumento. Relato escrito, commitando.
