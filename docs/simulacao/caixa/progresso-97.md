# Rodada 97 · Executora · progresso

- **21:15** · rodada aberta. `git fetch`: local igual ao `origin/main` em `cdf2658` (o `pull --rebase` recusou por causa do `lore/` sujo do Cartógrafo, e não havia nada a trazer). Despacho lido inteiro.
- **21:18** · `test-espelho` no topo (`cdf2658`), local: **exit 1, 7 falhas**, em 1v1-encostado (20260903), 1v1-cacada (as duas sementes), 4x4-media (as duas) e 4x4-aberto (as duas). Reproduz o CI.
- **21:20** · worktree destacada no scratchpad, em `0934136` (a última verde do CI), com `node_modules` por junção: `test-espelho` **exit 0**, 14 de 14 sem divergência.
- **21:22** · a mesma worktree em `6e8651e`: **exit 1, as mesmas 7 falhas**. As divergências por campo: `pv` 31, `chao` 11, `total`/`errouPor`/`danoBruto` 5 cada, `defesaPerdida` 5, `acao.pressao` 4, `veredito` e `absorcao` 3, `dados.acerto` e `dados.dano` 2, `danoLiquido` e `tick` 1. **A causa, lida no código:** `6e8651e` faz a mesa (`ajAtq` no `grid.astro`, `ataqueAtual` no `combate.astro`) somar `tierDe(...).penAcaoDados` (−1d6 em Grave, −2d6 em Crítico) no `dados` do acerto; o laço (`scripts/sim/motor.mjs`, `resolverContra`) passava `ajusteDados: 0`. A mesa rolava um dado a menos que o laço em todo golpe de quem estava Grave ou Crítico, e o resto (Vida, caído, Pressão, Defesa perdida) é a batalha seguindo por caminhos diferentes depois disso.
- **21:22** · **De que lado está o erro:** a MESA está certa. A §4f de `leitura-de-novato-decisoes.md`, itens 1 e 5, manda o `penAcaoDados` (0, 0, −1, −2) e manda o `ajAtq` somá-lo ao `dados`. O laço se alinha; nada muda para quem joga.
- **21:23** · controle da causa única, na worktree em `6e8651e`: só a linha do `ajusteDados` trocada para `penAcaoDados`, `test-espelho` **exit 0**. Uma causa, nascida em `6e8651e`.
- **21:26** · o conserto no topo (`motor.mjs`, `ajusteDados` a partir de `tierDe(...).penAcaoDados`, com o porquê no comentário): `test-espelho` local **exit 0**, 14 de 14. Então nada do que entrou depois (`888a196`, `4450055`, o piso condicional de `rolarExpr`, que é função compartilhada pelos dois lados) abriu segunda divergência. Worktree do scratchpad removida (junção desfeita antes, `node_modules` real intacto: 385 entradas antes e depois).
- **21:27** · item 0: a frase do G12 corrigida na seção 6 do `Pendencias.md` (o aberto é só o teto 4 da soma) e uma linha de estado no G12 do tema, que é de onde o "o Mestre está arbitrando" vinha; a F7 libera DOIS itens; o gerador lê `[ADIADO]` em qualquer ordem (lia só antes da outra marca), com o caso `[DECIDIR] [ADIADO]` no teste: o gerador da 96 falha nele, o novo passa.
