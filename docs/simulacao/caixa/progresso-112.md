# Rodada 112 · Executora · progresso

- **01:35** · rodada aberta: `status` vazio, `fetch`, `merge-base --is-ancestor` verdadeiro, `switch -C executora origin/main` em `b95bca6`. Despacho lido.
- **01:42** · feito na árvore: `gerar.py` emite `{por, preco:{pc}}` (função `formato_site`, aplicada na saída numa cópia, com o vocabulário novo); `copiar-economia.mjs` só copia, envelopa e separa a procedência; `--check` verde. Antes de escrever, o `--check` com o `gerar.py` novo já dava texto igual em `custo-de-vida`, `renda` e `pacotes` (o porte do formato para o Python é exato) e diferente só em `servicos` e `viagens` (o vocabulário). Comparação folha a folha contra a 111: 3076 folhas, 28 mudaram e todas são `por`/`nota`, zero valor. Despacho atualizado pelo autor no meio: `muda` virou `unidade`, `parto` virou `atendimento`; a missa, fora da lista, foi para `cerimonia` pela regra nova. Veredito da 111 (CORRIGE §4) aplicado junto, porque é o mesmo esquema: preço obrigatório, `null` só no teto da capital e no professor (com `ver` exigido); o gerador falha alto num preço que falta. Controles negativos acusados. `.gitignore` explícito e `README` com o como gerar, a âncora e a regra do vocabulário. Capítulo: 16 células mudaram, todas na coluna de unidade. Build verde, 13 valores lidos no `dist/`. Commitando.
- **01:44** · `bc7d3b7` publicado (rebase sobre `8da6352`). Relato escrito, contagens conferidas contra a saída da comparação. Commitando.
