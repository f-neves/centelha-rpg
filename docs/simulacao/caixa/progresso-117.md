# Rodada 117 · Executora · progresso

- **07:28** · rodada aberta: `status` vazio, `fetch`, `merge-base --is-ancestor` verdadeiro, `switch -C executora origin/main` em `db05cae`. Despacho lido.
- **07:30** · os cinco textos do autor colados só com formatação, os rótulos da calculadora ("Parte por caçador", e o que conta como forte e como fraca), e a nota do `recompensas.json` sem "Livre" ("a tarifa de base por caçador, por semana"). Build verde; as oito frases conferidas palavra por palavra no `dist/`; os 5 casos com os mesmos valores, na página. Relato escrito; commitando.
