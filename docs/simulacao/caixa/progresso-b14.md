# B14 fase 1 · Executora · progresso

- **08:55** · rodada aberta: `status` vazio, `fetch`, `merge-base --is-ancestor` verdadeiro, `switch -C executora origin/main` em `d884b4a`. Pedido lido (mensagem do Arquiteto).
- **08:55** · hash das saídas de hoje guardado (`inimigos.json` 6a51b18c…, `monsters.json` 829821ea…, `monsters-mesa.json` e03a5d9e…) e cópia em `../tmp/executora/b14/`. Lidos `gen-bestiario.mjs`, `gen-monsters.mjs`, `gen-deslocamento.mjs`, `ficha-schema.mjs`, `inimigos-custom.json`, `kael.json` e os leitores de cada satélite.
- **09:05** · `lib-bestiario.mjs` (a conta do stat block, sem mapa por id), `migrar-bestiario.mjs` (lê tudo do git no `d884b4a`; prova sozinho que o stat block de cada ficha é o do PIN) e as 309 fichas escritas; `gen-bestiario` e `gen-monsters` lendo a pasta. `cmp`: `inimigos.json`, `monsters.json` e `monsters-mesa.json` iguais aos de antes. `gen-elementos`, `gen-deslocamento` e `test-deslocamento` verdes.
- **09:07** · `criatura-schema.mjs` (zod, `.strict()`) e o bloco novo no `validate-data.mjs` (esquema, catálogos, vocabulário, concordância com o `elementos-bestiario.json`). Controle negativo: `willpowr`, `atiradr`, `courro` e uma fraqueza a mais no aboleth, cada um acusado; arquivos devolvidos e conferidos por `cmp`.
- **09:08** · `converter-imagens.mjs` passou a gravar o `imagem` nas fichas. Apagados, por ficarem sem leitor em código: `habilidades-bestiario`, `dimensoes-bestiario`, `lore-bestiario`, `ecologia-bestiario`, `imagens-bestiario`, `categoria-extra` e `conversao-extra`. `inimigos-custom.json` esvaziado (a criatura dele virou ficha). Regerado de novo: os três JSON seguem iguais por `cmp` e pelo sha256; `migrar-bestiario --check` diz que a pasta é byte a byte a que ele escreve.
- **09:09** · `docs/bestiario/ficha-criatura.md` escrito; nota de mudança de fonte no `Bestiario_Centelha.md` e a linha do `conversao-monstros.html` no `docs/MAPA.md`. Rodando `validate`.
- **09:10** · `reapontar` moveu 4 citações; as 3 que apontavam `COURACA` e `const ini` no `gen-bestiario.mjs` foram para o `lib-bestiario.mjs` à mão (o mapa do diff não segue código que muda de arquivo). `npm run validate` verde. Rodando `build`.
- **09:14** · `build` de novo verde, a nota nova da couraça em `dist/mesa/referencia/`, os três JSON ainda iguais por `cmp`; `test-editor-bestiario` verde. Relato `b14-executora.md` escrito. Commit.
