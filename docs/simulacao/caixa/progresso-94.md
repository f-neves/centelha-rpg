# Rodada 94 · Executora · progresso

- **20:04** · rodada aberta. `git pull --ff-only` ok, HEAD `818b8b6`. Árvore com `lore/` sujo do Cartógrafo (não encosto). Despacho lido inteiro.
- **20:06** · MEDIDA 1, contagem. A diferença do despacho (A 19/20, I 10/12, K 18/19) é a caixa `- [~]` (parcial), que o grep de `- [ ]`/`- [x]` não pega e o índice conta como aberto: A11, I11, I12, K28. Somando `[~]`, as 12 linhas da tabela do índice batem com as caixas (A 19+1+8=28, I 10+2+2=14, K 18+1+11=30; os outros sem `[~]`). Total 249 = 172 `[ ]` + 4 `[~]` + 73 `[x]`... conferir no script. O parágrafo do índice que diz "mais os cinco E4-E8, mais E9, E10, J9, J10, mais a nomeada" é prosa velha: esses já estão dentro da tabela (E tem 10 itens, J 11), então a prosa conta em dobro.
- **20:06** · MEDIDA 1, anomalias que a contagem por caixa esconde: siglas repetidas A22 (aberta `[ ]` e fechada `[x]` no mesmo arquivo), I5 (dois itens diferentes com a mesma sigla), L6 e L9 (versão `[x]` e versão `[ ] ~~riscada~~`, e a riscada conta como aberta). Dois itens sem sigla: G "As cinco físicas de toda sessão" (fechado) e J "A linha de fechamento do `test-grid`" (aberto, nomeado de propósito).
- **20:06** · MEDIDA 2, formato. Regular: todo item é `- [ ]`/`- [x]`/`- [~]` na coluna 0, seguido de `**SIGLA · ...**` (às vezes dentro de `~~ ~~`), sigla `[A-L]\d+[a-z]?`. Nenhuma caixa aninhada nos 12 arquivos. A marcação `[DECIDIR]`/`[FAZER]`/`[AUTOR]`/`[CONSERTAR]` é regular em A a K; no L ela vira texto livre (`[FEITO em ...]`, `[ANOTADO ...]`, `[ESCALADO ...]`). Dá para um script ler.
- **20:06** · MEDIDA 3, citações por linha do `Pendencias.md`: UMA em todo o repositório (fora de `lore/`), `04-revisora.md:30`, "Pendencias.md linha 1344", que é do arquivo de antes da divisão de 17/09 e já não aponta para nada. Nenhum script lê o `Pendencias.md` (as menções em `scripts/` são comentário). Reescrever não desloca nada vivo.
- **20:20** · três subagentes, só leitura, voltaram (saídas no scratchpad desta sessão: `L-itens.tsv`, `suspeitos-A-K.tsv`, `jogador-novo.md`). (1) L: 103 itens, 70 `[ ]`, 33 `[x]`, 0 `[~]`; 20 abertos cujo próprio texto diz que algo foi feito (a maioria fecha só em parte). (2) A a K: 9 "fechados com prova" (A22, B13, C3, D1, E9, E10, J4, K12, K13) e 8 suspeitos (K28, K20, K27, G12, G13, C4, E3, K5). (3) jogador novo: 47 M (32 feitas, 7 decididas esperando mão, 6 abertas, 2 descartadas), 104 C (78 com sha, 12 resolvidos sem sha, 4 abertos, 3 parciais, 3 falso-fechados C-22/C-39/C-61); das seis M "esperando mão", M-05 (`7db14f1`) e M-42 (`2520b5d`) JÁ FEITAS fora das rodadas; item 9 de Relações Sociais FEITO em `6509801`, veredito `a6e7e41`. Nada disso usado ainda: vou conferir amostras antes.
- **20:20** · amostra dos subagentes conferida: os 12 shas citados como prova existem e as mensagens dizem o que eles disseram (`888a196` B13, `971b6f4` D1, `b903a26` E9, `fb9310c` E10, `20daeea` J4, `b6af150` K13, `7db14f1` M-05, `2520b5d` M-42, `6509801` e `a6e7e41` item 9, `4dac366` M-10, `976afa8` M-36); `regras.json` tem `acoes.longevidadeFirula.intervaloBaseDias: 8`.
- **20:22** · dossiê lido pela ferramenta `Artifact` (read), versão `1789563971-21e8`. A página guarda as marcas republicando a si mesma num `<script id="estado">`, sem banco (capacidade só `artifact`, então `ArtifactData` não se aplica). **Na versão de hoje o estado é `{"marcas":{}}`: nenhuma marca do humano gravada.** O dossiê tem 19 perguntas (M-09, 17, 18, 20, 23, 27, 36, 38, 45, 46, 15, 16, 34, 35, 39, ...); várias já foram decididas depois.
- **20:22** · FORMA escolhida: GERADA. O formato é regular (medida 2), então a contagem e a tabela de itens saem de `scripts/gen-pendencias.mjs` entre dois marcadores, com `--check`; à mão fica só a direção. O gerador também ACUSA, sem consertar, sigla repetida, item aberto riscado e item sem sigla (8 hoje). Opção `--raiz` para rodar sobre cópia.
- **20:23** · gerador rodado numa cópia no scratchpad: 249 itens, 169 abertos, 4 parciais, 76 fechados (bate com o índice velho: 169 + 4 = 173 abertos). Títulos de negrito que quebram linha (L29, K28) juntados; L62, cujo negrito é só a etiqueta, sai com o texto dela.
- **20:23** · ENSAIO DOS TRÊS SENTIDOS, na cópia (nenhum arquivo real tocado). 0) base: `--check` exit 0. 1) vermelho com o defeito: A1 virou `[x]` no tema e o índice não foi regerado: exit 1, "fora de sincronia". 2) verde com o conserto, sem tocar no gerador: regerado, `--check` exit 0 (168 abertos, 77 fechados). 3) vermelho de novo com a regressão: A1 volta a `[ ]` no tema e o índice fica: exit 1. A cópia do tema voltou idêntica ao original (`cmp`).
- **20:26** · REVISÃO: das 9 "fechadas com prova" do subagente, conferi eu mesma cada prova e fechei **7 caixas** nos temas, com uma linha de prova no fim do bloco de cada uma: A22 (a entrada de antes da decisão; a decisão é a outra A22), B13 (`888a196`), D1 (`971b6f4`; nenhuma Técnica tem `banda` hoje), E9 (`b903a26`; a `aparencia.nota` já diz o modelo novo), E10 (`fb9310c`; a seção da M-09 está marcada), K12 (§14/§16 e `c5dd390`), K13 (`b6af150`; cabeçalho do `lib-tempo.mjs`). Ficaram SUSPEITAS, sem mexer: C3 e C4 (a M-10 as esvazia, mas é cancelamento, não entrega), J4 (`20daeea` consertou o mesmo defeito do B12, mas o J4 é `[DECIDIR]`), K5, K20, K27, K28, G12, G13, E3, e as 20 do L cujo texto diz que algo foi feito. Nenhuma linha dos temas deslocou citação viva (a única citação por linha a esses arquivos é de `progresso-41-l72.md`, histórica).
- **20:27** · `jogador-novo-bestiario.md` (fonte dos C-102 a C-104) está versionado; `lore/economia/` não está.
- **20:28** · `Pendencias.md` reescrito: seções 1 a 6 à mão (frentes, pronto para executar, com o humano, bloqueios, revisão com fechados e suspeitos, ordem proposta marcada como PROPOSTA DO ARQUITETO), seção 7 entre os dois marcadores. Nenhum `arquivo:NNN`. A lista dos [DECIDIR] NÃO foi repetida à mão (seria segunda lista); a seção 3 aponta para a coluna Marcação da tabela gerada.
- **20:29** · gerador rodado no arquivo real: 249 itens, 162 abertos, 4 parciais, 83 fechados (as 7 caixas fechadas com prova), 8 anomalias. `gen-pendencias.mjs --check` entrou no `validate` logo depois do `gen-cap-itens`; `npm run validate` exit 0, e o `test-portoes` contou o gerador novo sozinho ("11 de 16 geradores com `--check`"). Travessão: zero no `Pendencias.md`, no gerador, nos cinco temas tocados e neste progresso (lidos com `grep -c` no arquivo).
- **20:30** · primeira tentativa de commit RECUSADA pelo gancho: "'nodescripts' não é reconhecido". O conserto do espaço que faltava no `package.json` (defeito meu da rodada 93, `5134d6c`, achado por `git log -S`) saiu "`&& nodescripts`". Nada commitado.
- **20:32** · refeito com o trecho ancorado em palavras dos dois lados; conferido por script que as 56 partes do `validate` têm a forma `node scripts/<nome>.mjs`; `npm run validate` exit 0. Relato `94-executora.md` escrito.
