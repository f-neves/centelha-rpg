# Rodada 92 · Executora · progresso

- **17:35** · rodada aberta. `git pull --ff-only` ok, HEAD `05a9f84`. Árvore com `lore/` sujo do Cartógrafo (não encosto). Despacho e `91-revisora.md` lidos.
- **17:36** · item 1: `aguentouFicarParado(total, d)` em `artes-grid.ts`; `oferecerSaida` chama a função e não compara mais nada no ramo do ficar parado. Asserção nova (borda: 5 não aguenta, 6 e 9 aguentam). Positivo exit 0. **Controle negativo**, a mesma troca da Revisora, agora dentro da função (`total > d.difMetade`): exit 1, a asserção nova caiu com `false false false`. Arquivo restaurado, teste de novo exit 0.
- **17:38** · itens 2 a 4: `regras.json` cita o núcleo (4 m, 25 vira 13); a fronteira do Resistir ganha "os do mundo (... que não vêm de uma Arte)"; `L-simulacao-simultaneo.md:5492` agora diz `src/lib/artes-grid-mesa.ts:1901` (conferido: é o segundo `Math.max(0, (alvo.pv_atual ?? 0) - golpe.liquido)`). `reapontar.mjs` moveu 1 citação de `artes-grid.ts` (+10) e ela saiu de 1883 para 1893, mas o alvo está em 1894: já estava um abaixo em `84228f3` (1864 contra 1865). Corrigida à mão para 1894. `tsc` limpo, `validate` e `build` exit 0, 23 capítulos, "25 vira 13" no HTML de `/artes/regras`.
