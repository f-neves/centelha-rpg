# Rodada 109 · Executora · progresso

- **20:35** · rodada aberta: `status` vazio, `fetch`, `merge-base --is-ancestor` verdadeiro, `switch -C executora origin/main` em `0d1ae49`. Despacho lido.
- **20:37** · build de `0d1ae49` (sem mudança de `src/` desde `050ba5f`), prova no gerado: `dist/ref-index.json` 20:37:18, `dificuldade` com `["Dificuldade","dif"]` e `integridade` sem "compostura". Palavras com mais de um dono autolinkável no índice: medindo agora.
- **20:40** · instrumento da 109 pronto em `../tmp/executora/`: `gerar-inj.mjs` gera o `autolink-inj.js` a partir do `autolink()` de `Referencias.astro:38-94` (só tira os tipos; três ganchos desligados por padrão), e `p109.mjs` mede em quatro modos. Ensaio em 3 páginas: a passada injetada sem opção reproduz o site por chave (184 = 184, zero diferença em ficha e defesas); a 2ª passada que semeia o `used` leva o bestiário a 1670, igual por chave ao bestiário montado inteiro. Rodando as 107 páginas.
- **20:56** · `real-semear` (107 páginas; as 6 da `/mesa` com `data-refs=off` não assentam e ficam fora das outras rodadas) e `real-ingenua` (101 páginas) medidos. Só duas páginas ganham texto no `main` depois do 1º link: `bestiario` e `ficha` (nesta volta a ficha tinha 8 links quando o autolink rodou, e 95 com a página montada). Rodando os modos `inj-base` e `inj-dono2`.
- **21:07** · `inj-base` e `inj-dono2` medidos (101 páginas cada). **Controle:** a passada injetada sem opção dá, por chave, exatamente o autolink do site em 99 páginas (4346 = 4346, zero chave diferente; fora `bestiario` e `ficha`, que dependem da hora). 2ª passada ingênua: 646 links duplicados em 47 páginas, e fora do bestiário ela só acrescenta duplicata. Medindo o custo no bestiário.
- **21:16** · custo medido (bestiário, índice bloqueado, 5 repetições, mediana): passada sobre 40 fichas 102 ms, sobre as 309 fichas 708 ms, 2ª passada semeada depois de montar 691 ms; com a CPU 4x mais lenta, 463, 3311 e 3170 ms. Ficha repetida 11 vezes, mais 5 com a CPU 4x: sempre 95 links; a perda de 95 para 8 da rodada completa não se repetiu. Parte 2: as 175 ocorrências que a regra mata foram lidas uma a uma, com o contexto na posição exata do link (`ctx-dono2.txt`); a ficha e o bestiário conferidos no DOM (Esquiva e Bloqueio da ficha estão na lista de Habilidades de Combate).
- **21:19** · classificação das 175 conferida por script contra a tabela (89 errados, 61 certos, 25 limítrofes; bate linha a linha). Relato escrito. `git status`: só o relato e o progresso; nenhum arquivo versionado de código tocado. Commitando.
