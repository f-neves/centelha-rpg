# Rodada 95 · Executora · progresso

- **20:44** · rodada aberta. HEAD local `a74793d` (o `main` local está à frente do `origin` com commits do Cartógrafo; não empurro, por ordem do Arquiteto). Árvore com `lore/` sujo do Cartógrafo (não encosto). Despacho e `94-revisora.md` lidos inteiros.
- **20:45** · gerador alterado: linha com cara de caixa (`^\s*([-*+]|\d+[.)])\s*\[.?\]`, a régua da Revisora) que o formato de item não casa é RECUSADA nos dois modos (exit 1, com a linha, sem escrever); cerca de código (``` ou ~~~) pulada; sigla de letra diferente do tema acusada como anomalia; junção de título só quando o negrito foi aberto, parando em linha em branco, cabeçalho, cerca ou cara de caixa. Repositório real: `--check` exit 0, a saída idêntica à de antes (249, 162, 4, 83, 8 anomalias).
- **20:46** · `scripts/test-gen-pendencias.mjs` novo, com os onze casos. Com o gerador novo: exit 0, 18 de 18. **Controle negativo** com o gerador de `HEAD` (`--gerador`): exit 1, **15 falhas** (as 12 das seis linhas recusadas, e o título, a sigla de outro tema e a cerca). Passam com os dois a base, o `[X]` maiúsculo e a sigla minúscula, que o gerador velho já tratava. O `--check` do teste roda sobre um índice gerado ANTES da linha plantada. Sem isso, o índice vazio daria vermelho em qualquer gerador. Conferido à mão: com o gerador velho e o subitem plantado, o `--check` sai **exit 0** ("em dia", 1 item), que é o verde calado da 94; com o novo, exit 1 apontando a linha 4.
- **20:46** · teste no `validate`, logo depois do `test-reapontar`. Trecho da edição ancorado em palavras dos dois lados; as 57 partes conferidas por script.
- **20:47** · as três notas no `Pendencias.md`. Nota 1: saiu a causa não testada ("veio de um `grep`"); fica o fato, conferido em 23/09 no fonte, e o C-39 também no HTML (pela Revisora); "não se sabe por que foram marcados". Nota 2: seções 1 e 2 dizem o que cada rodada fez (livro na 90, ajustes na 91 e na 92, "ficar parado" na 91 com a comparação no motor na 92; a 93 foi o `reapontar.mjs`). Nota 3: a seção 6 diz que a §17 fecha a frente NO LIVRO, e que o motor segue sem o teste de Frenesi. `--check` verde.
- **20:47** · `npm run validate` exit 0: "gerador do Pendencias.md OK", "os 63 testes estão no `validate` ou no `smoke`", "11 de 16 geradores com `--check`". Travessão zero nos quatro arquivos tocados.
