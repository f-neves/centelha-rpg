# Progresso · rodada 82 · o eco da premissa que caiu, o portão que vigia o arquivo, e a ressalva

Sinal de vida, não resumo. Uma linha por etapa pequena, no instante em que ela fecha, hora lida
da máquina. Etapa sem hora é honesta; hora inventada não é.

Ancorada em `70ea0df`. Três itens: o eco no `progresso-79.md` e no `L99` (e o texto novo não pode
afirmar o que a `M-21h` ainda não decidiu), o portão passando a vigiar o `regras.json` INTEIRO em
vez de quatro blocos, e o texto da ressalva dos 23, que avisava contra o risco errado.

**A placa do Reiniciar não entra:** ela depende da `M-21h`, que é da mesa.

- **02:28** · começo. `HEAD` = `70ea0df`, árvore limpa fora o `jogador-novo-prompt-executor.md`.
  A run **35058991317** (`3546d0b`, a rodada 81) ainda está `in_progress`, sem nenhum trabalho
  vermelho até aqui, com o `watch` de pé.
- **02:29** · item 1 FEITO nos dois lugares, e o texto novo **não decide nada**: o
  `progresso-79.md` e o `L99` passam a dizer só o que é verificável hoje (existe uma quinta
  escrita de Vida na interface, ela não passa pela trava, e se é legítima está na `M-21h`). Os
  dois registram que a PRIMEIRA correção trocou uma afirmação falsa por outra mais estreita, e
  que o "vem acompanhada de zerar relógio e condições" era o padrão da tela e não uma necessidade.
- **02:29** · e a PLACA no código perdeu a mesma premissa, que era o terceiro lugar onde ela
  estava escrita. Não escrevi a placa nova (essa depende da `M-21h`): tirei a afirmação que caiu
  e deixei o que se pode afirmar. Se o teu "a placa não entra nesta rodada" incluía não encostar
  nela, é um bloco para reverter · o que eu não conseguia era consertar dois documentos e deixar
  a mesma frase falsa no código para onde eles apontam.
- **02:29** · item 2 FEITO: o portão passou a vigiar o `regras.json` **inteiro**, e não uma lista
  de blocos. Custou UMA frase reescrita, a que já estava nomeada desde a rodada 63:
  `arcano.fonteElemental.notaAr` dizia "o elemento menos letal" e passou a dizer "o elemento que
  menos mata". Medido antes: 35 blocos de topo, e uma única ocorrência de "letal" no arquivo.
- **02:30** · ensaio do item 2, com a falsificação DELA: `"Cortante e Perfurante sao Letal"`
  plantado em `dano.nota` → `EXIT=1`, nomeando `regras.json`; sem o plantio, verde; restaurado,
  verde de novo.
- **02:30** · item 3 FEITO: a ressalva dos 23 passou a avisar contra o risco MEDIDO, e não contra
  o que eu supus. O risco não é "item fechado contado como aberto", é **"item menor do que a
  linha dele diz"** (o `C-70`, que encolheu de "escrever a regra" para "acrescentar três
  palavras"). **Conferi a citação dela em vez de herdá-la:** `habilidades.md:70` diz mesmo "o
  Atributo (Destreza ou Força)", lido no arquivo. E está escrito por que a amostra NÃO foi
  estendida aos 23.
