# Rodada 115 · Executora · progresso

- **06:32** · rodada aberta: `status` vazio, `fetch`, `merge-base --is-ancestor` verdadeiro, `switch -C executora origin/main` em `7cd7dfe`. Despacho lido.
- **06:38** · feito na árvore, sem commit: B14 registrada; "Caça e recompensas" na página de Serviços (texto do autor, tabelas de degrau e de capacidade em blocos gerados); "Semanas de aventura" trocada por "Recursos durante a aventura" na página 1, e o link do Ofício e Mundo trocado; a recompensa no modelo (`modelo.py`, `gerar.py`, a régua do `arred` em tabela) e `src/data/recompensas.json` pelo `copiar-economia`; esquema `.strict()`; `src/lib/recompensa.ts`, a calculadora (`src/pages/recompensa.astro`, `CalculadoraRecompensa.astro`), `FERRAMENTAS`, `scripts/test-recompensa.mjs` no `validate`. **Caso 2 diverge:** exata 2.250, e o `arred` (passo 100 acima de 1.000) dá 2.300; os outros quatro batem, na função e na página. Perguntei ao Arquiteto, e nada é commitado até a resposta.
- **06:41** · rodada 116 aplicada na mesma árvore (despacho `47ac4ac`): `modelo.py:200` passa ao `inteiro`, e tudo foi regerado (o `recompensas.json` já sai com o Livre novo). Os 9 Livre/Sem e Livre/Ano batem com o despacho. **O Custo/Sem muda junto em 3 faixas** (Custo = Renda − Livre): Doutor 780 → 781, Abastado 1.345 → 1.344, Aristocrata 4.090 → 4.086, e o estilo de vida do pacote familiar delas também. Avisei o Arquiteto. Nenhuma ocorrência dos valores antigos no texto corrido nem no `estado-revisao.md`. Esperando a resposta sobre o caso 2 da 115 e sobre o Custo antes de commitar.
- **06:47** · a 116 foi publicada em separado (`39e558e`): guardei os arquivos da 115, voltei os rastreados ao `HEAD`, apliquei só a linha da 116 e commitei. Depois pus a 115 de volta por cima (sem o `Pendencias.md` velho, que foi regerado por causa do `e9a41f1` do Arquiteto na A28) e regerei tudo; `renda.json` e `custo-de-vida.json` ficaram iguais ao `HEAD`. A tabela de capacidade já sai com o Livre/Ano novo (Doutor 1.404, Aristocrata 3.192). Segue parado só o caso 2.
- **06:52** · o autor decidiu o caso 2: a bolsa segue sempre o `arred`, 2.300. Teste corrigido; os 5 casos batem na função (`test-recompensa.mjs`) e na página. Nenhum exemplo de bolsa acima de 1.000 pc no texto do capítulo (o único número assim fora de tabela é a capacidade da aldeia, 12.000 pc, que não é bolsa). Build verde; tabelas de degrau e de capacidade, links e a calculadora lidos no `dist/`. Commitando.
