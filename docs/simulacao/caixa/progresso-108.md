# Rodada 108 · Executora · progresso

- **14:04** · rodada aberta: `status` vazio, `fetch`, `merge-base --is-ancestor` verdadeiro, `switch -C executora origin/main` em `626cc4e`. Despacho lido. Medidores da 104 copiados para `../tmp/executora/` (`medir-autolink.mjs` e `medir-autolink-rev.mjs`). O Atributo Compostura tem id `compostura` no `ref-index` (`tipo: atributo`, url `regras/atributos`).
- **14:06** · livro lido atrás de "Compostura": nenhum capítulo usa a palavra no sentido da Integridade (`habilidades.md:41` separa as duas: "Esconder o que sente é assunto de Compostura; Integridade é não ceder"). No sentido comum da palavra, minúscula, há 5 nos dados (a definição da própria `integridade` no glossário, 3 textos de técnica e a conduta da Temperança). "alvo": nenhum "número-alvo"/"valor-alvo" no livro; o sentido de Dificuldade aparece só em "acima do alvo" (definição de Margem) e na definição da própria Dificuldade. Build limpo do código de hoje feito (`build-antes.log`), medição 'antes' rodando com o medidor novo `medir-108.mjs` (o da Revisora na 104, lendo todos os `a.ref`).
- **15:47** · (retomada: fiquei parada de 14:10 a 15:47 esperando o aviso da tarefa em segundo plano, que não chegou; daqui para frente a medição longa roda dentro do turno.) Medição 'antes' (`m-antes.json`, gravada 14:10): 107 páginas, **43 links "Compostura" para `glossario#integridade` em 25 páginas** e **278 "alvo" para `glossario#dificuldade` em 43 páginas**, os números da 104. O Atributo já recebia 8 "Compostura" (`regras/atributos`). 6 páginas da `/mesa` não pedem o índice e não estabilizam (arquivos, combate, diario, grid, grupo, mapas); ficam fora da comparação. Conserto da Compostura aplicado (`integridade` com `aliases: []`), build limpo e medição 'depois1' rodando.
- **15:53** · 'depois1' (107 páginas, 289 s, as mesmas 6 da `/mesa` sem índice, 0 erro): **zero "Compostura" para `integridade`**. Foto por chave contra 'antes': **38 reencaminhados** (`glossario#integridade` para `regras/atributos`, mesma página e palavra); **5 saídas sem par** (arvore, bestiario, ficha, aparencia-virtudes-vontade, tecnicas), todas em escopo que já tinha um link do Atributo mais adiante: pela ordem do documento, o Atributo passa à primeira ocorrência (a que era da Integridade) e a ocorrência seguinte fica sem link, porque é um link por verbete por bloco; **2 entradas sem par**, "Integridade" para `glossario#integridade` em bestiario e ficha, a vaga da Integridade no escopo deixou de ser gasta pela "Compostura". Nenhum outro verbete mudou. Commitando a Compostura.
- **15:55** · `a26fda1` publicado (fast-forward de `626cc4e`), gancho verde. O "alvo": nenhuma expressão de dono único no livro ("número-alvo", "valor-alvo" não existem; "acima do alvo" só na definição de Margem), então a palavra só sai: `dificuldade` fica com `aliases: ["dif"]`. Build limpo e medição 'depois2' rodando.
- **16:01** · 'depois2' (107 páginas, 288 s, as mesmas 6 sem índice): **zero "alvo" para `dificuldade`**; saíram os 278, sem par. Entraram 134 sem par, e **131 deles são todos no `bestiario`** (defesa, absorção, iniciativa, porte, velocidade, magia...). **O `bestiario` é ruído do instrumento, e não do conserto**: medido 4 vezes seguidas no MESMO `dist/` deu 493, 882, 747 e 493 links (a última esperando 20 leituras paradas, e não 3). O autolink roda uma vez, e pega os cartões que existirem naquele instante. Nas 'antes' e 'depois1' ele deu 617 as duas vezes, por sorte. Fora do `bestiario`, entraram **3 links "Dificuldade"/"Dif" para `dificuldade`** (mestre, acoes-e-sistema, coracao-do-sistema), cada um no mesmo bloco onde um "alvo" gastava a vaga do verbete ("o total supera o alvo: a Defesa de um inimigo ou a Dificuldade de uma tarefa"). Nenhum outro verbete mudou. Commitando o "alvo".
- **16:02** · `050ba5f` publicado (fast-forward de `a26fda1`), gancho verde. Controle negativo rodando: `glossario-antes.json` posto de volta só para medir, build limpo, medição, e o glossário novo restaurado no mesmo comando.
- **16:11** · controle negativo: reproduziu 43 em 25 páginas e 278 em 43; contra 'antes', zero diferença fora do `bestiario`. Glossário novo restaurado (md5 igual ao de `050ba5f`). Build limpo final e `validate` verdes. Relato escrito, commitando.
