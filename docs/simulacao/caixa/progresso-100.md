# Rodada 100 · Executora · progresso

- **00:07** · rodada aberta. `git fetch`: `HEAD` = `origin/main` = `2ac8866` (o despacho). A 99 fechada antes (`bed1e91`, `64559c1`, CI success no run inteiro). Despacho lido: só medição, nada fora da caixa e do L104.
- **00:09** · lidos os três logs de falha (`--log-failed`) e o bloco do mover de `test-grid.mjs` (1155-1260) com o `pontos()` (100-182) e a bancada de `mesa-mock.mjs` (peças, posições, ações: tudo determinístico, sem `Math.random`). A falha é sempre na **cena de 30** (a de 12 passou nas três). Próximo: logs dos runs verdes, para ver que peça a cena de 30 escolhe quando passa.
- **00:11** · a iniciativa da cena é rolada no teste (`ini-rolar`, `grid.astro` `rolarIniciativas`, d6 de verdade): é a única fonte de acaso antes do mover, e é ela que decide quem está na vez. Montada uma sonda FORA do repositório (cópia do `test-grid.mjs` no scratchpad, só a cena de 30, N voltas, e uma linha SONDA antes do mover com as peças na vez, onde estão e o que o ponteiro pega no centro delas). Rodando 12 voltas.
- **00:23** · sonda, 10 voltas locais da cena de 30: **0 falhas em 10**. Em todas, quem está na vez são as peças grandes (as `mon-aboleth`, `i % 3 === 2`: Criatura 6, 9, 12 ... 30, 115 px) e às vezes a Criatura 22 (pequena, dentro do palco, com o centro coberto pela Criatura 9). Coleta dos 70 últimos runs do CI: 67 com a linha do mover na cena de 30, 3 falhas (as do L104) e 64 verdes, e nos verdes a escolhida varia (Criatura 6, 9, 12, 15, 18, 21, 24, 27, 30), sempre uma aboleth. Segunda sonda, 30 voltas, agora com a lista de peças pegáveis e não pegáveis e a rolagem da iniciativa.
- **00:25** · primeira volta da segunda sonda: as peças **não pegáveis** (os 9 pontos do `pegaEm` cobertos por outra peça) são exatamente C7, C10, C13, C16, C19, C22, C25, C28 e H4, isto é, `i % 3 === 0` com `i >= 3`: a peça pequena logo depois de uma aboleth (115 px) na fileira de 3 em 3 colunas da bancada. H1 é a única `i % 3 === 0` pegável (não tem aboleth antes). A Criatura 17 é águia, pegável, dentro do palco, e é a primeira pegável do palco na ordem do DOM: é o `pegaveis[0]` do desempate, e por isso a escolhida é sempre ela quando ninguém na vez é pegável.
- **00:45** · segunda sonda, 30 voltas: **1 falha em 30, e não é o L104** (volta 24, o teto de 60 KB de HTML por movimento, 67,1 KB). A asserção `[aquece]` passou 30 de 30, e somando a primeira sonda, 40 de 40. Em 30 de 30 voltas, quem está na vez no mover é exatamente o conjunto do Tick 1 da rolagem (nada entre a rolagem e o mover mexeu nele). Em 8 das 30 havia peça na vez não pegável, e em todas havia também uma aboleth na vez, que a escolha pegou. O Grid tem `?semente=N` (`acaso.ts`), que torna a rolagem repetível: próximo passo é achar uma semente em que a vez caia só nas pequenas cobertas, e ver se a asserção cai.
- **00:50** · com `?semente=` a cena é repetível (semente 1 deu a mesma rolagem nas duas voltas). Três sementes (1, 2, 3) bastaram para achar o deslocamento (4 dados antes da rolagem) e a ordem da fila, a menos de 192 permutações coerentes. Nas sementes 4 a 3000, **9** põem a vez só em peças cobertas em TODAS as 192 permutações. Duas delas foram escolhidas para o controle positivo: 1369 (prevista: Criatura 13 e 22) e 767 (prevista: Criatura 13, 22 e 25).
- **01:01** · controle positivo: **semente 1369 caiu 2 de 2** com a assinatura exata do run 35911397511 (1 no palco, 2 no tabuleiro); **767 caiu 1 de 1** com a do 35914287185 (2 no palco, 3 no tabuleiro); **1369 com espera de 3 s antes da leitura caiu também** (não é tempo). Nenhum commit entre `2ac8866` e `6ee7f79` (o HEAD testado) mexe em `src/` ou `scripts/`. Linha no L104 escrita, `gen-pendencias` rodado (`Pendencias.md` não mudou), relato escrito.
