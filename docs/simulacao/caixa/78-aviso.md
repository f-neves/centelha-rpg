# Rodada 78 · aviso de revisão · a regra geral que ninguém decidiu sai do dado, e o gancho passa a dizer o que faz

**Este arquivo é o aviso, e o sha dele é o sha do aviso.**

## Os quatro campos

| campo | sha |
|---|---|
| **BASE** | `680964f` · o `L98` registrado |
| **SHA do trabalho** | `2f74e1d` · a faixa é `680964f..2f74e1d`, um commit só |
| **SHA do aviso** | este commit |
| **TOPO do repositório** | conferido por `git rev-parse origin/main` ao escrever |

## O que a rodada prometeu

Os seus quatro `CORRIGE` da rodada 77, mais a medição do portão de vocabulário, que era para ser
MEDIÇÃO e não construção.

## O que eu quero que você julgue, e são quatro coisas

**1 · O conserto dos dois textos, e a pergunta é se ele ficou CERTO e não se ficou diferente.** A
frase nova diz que o punho atravessa o zero *"mesmo contra quem o pararia nele"*. Isso é uma
terceira redação, nem a antiga nem o corpo da decisão: ela descreve o Inquebrantável sem nomeá-lo.
**Um leitor que só tem o `tecnicas.json` na mão entende quem é "quem o pararia nele"?** Regra
comprável se lê sozinha, e o comprador do `mao-de-ferro` pode nunca ter ouvido falar da outra
Técnica.

**2 · O recorte do gancho, agora com quatro arquivos, e um deles não estava no seu achado.** Ela
acrescentou `package-lock.json` por conta própria, com o argumento de que ele fixa a versão que o
`package.json` só declara. **Eu aprovei**, e o que eu quero de você é o outro lado: existe algum
arquivo que MUDA o resultado do `tsc` e continua fora? Ela nomeou três, você nomeou os mesmos
três, e os dois podem estar olhando a mesma lista.

**3 · O `>/dev/null` virou captura, e a sua falsificação foi refeita.** Confira que a saída
aparece **na falha** e não sempre, porque um gancho que fala a cada commit é um gancho que ninguém
lê. E confira o `HEAD` antes e depois, que ela diz ter conferido.

**4 · A medição do item 6, que é a que decide se o portão nasce.** Ela mediu: 7 catálogos, 6.235
campos, 1,98 ms por passada, e **dois falsos positivos em 6.235**, os dois "ambientes letais", que
são português comum. E mediu o que ele não pega: a `imortalidade-tenue` ("só a destruição total o
mata") **não casa padrão nenhum**, então um portão de vocabulário teria pego as cinco que usavam a
palavra e deixado passar a sexta.

**A minha decisão, e eu quero que você a ataque:** o portão entra, mas **com padrão de FRASE e não
da palavra solta** (`dano letal`, `trilha letal`, `limiar de morte`), para que "ambientes letais"
continue sendo português legítimo em vez de ser reescrito para caber no instrumento. E entra com
uma linha ao lado dizendo o que ele não vê, com a `imortalidade-tenue` nomeada como o caso medido.
**O que eu quero que você meça:** o padrão de frase deixa passar alguma das cinco que a rodada 77
consertou? Se deixar, ele é mais estreito do que o problema e a minha decisão está errada.

## O que NÃO é seu

O `L97` (o `test-grid` intermitente) e o `L98` (o `.astro/` compartilhado) estão registrados e não
são desta rodada.
