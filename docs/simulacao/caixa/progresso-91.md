# Rodada 91 · Executora · progresso

- **17:11** · rodada aberta. `git fetch` ok, HEAD `26f0d59` igual a `origin/main`. Árvore com `lore/` sujo do Cartógrafo (não encosto). Despacho e o bloco "Quatro decisões a mais" da §16 lidos. Números de referência do despacho reconferidos de cabeça contra a conversão (2d6 > 5 = 72%, 3d6 > 5 = 95%, 2d6 > 8 = 28%, 3d6 > 10 = 50%).
- **17:12** · nenhum teste exercitava `paresDeCoragem`/`oferecerSaida` (busca em `scripts/` e `src/` por `paresDeCoragem`, `oferecerSaida`, `parado:`, `difMetade`). Função que decide extraída para `artes-grid.ts` (`opcoesDeFicarParado` e `Desvio.difParado`), PRIMEIRO com a semântica antiga (Virtude + Atributo, contra `difMetade`), e asserções novas em `test-artes-grid.mjs`: **controle 1, falhou 3 de 3** (veio `10 · 15 · 20`, `Bravura + Vigor 7`, `8 50 0 0`).
- **17:13** · semântica nova (Virtude sozinha, `Math.ceil(difMetade / 2)`): `test-artes-grid` exit 0. **Controle 2** (`git stash push --` dos dois arquivos meus de `src/lib`, teste novo mantido): exit 1, `M.opcoesDeFicarParado is not a function`; `stash pop` limpo, `stash list` vazio. `artes-grid-mesa.ts` passa a chamar a função e rolar contra `d.difParado`. `regras.json:1995` reescrito (único texto que diz essa Dificuldade; aparece em `artes/regras.astro:363` e `mesa/referencia.astro:154`).
- **17:15** · grupo 2: frase nova no teste de Virtude. Achado ao escrever: os dois `Vontade + Convicção` de `efeitos.json` (Banir e Círculo) NÃO são resistência do corpo, são o espírito que rola para ficar e a criatura que força a linha. Escrevi "corpo" só para o `Vigor + Convicção` e citei os dois à parte, "continuam como estão". Grupo 3: parágrafo em `racas.md` depois do custo de entrar (fúria racial não ativa as Técnicas "em fúria"; a Técnica Frenesi é outra coisa). Grupo 4: `racas.json` meio-orc sem o "não é raça jogável" e com "pouco mais de 70 anos" (como o capítulo). Os "60 anos" que sobram em `src/` são do Orc puro, e batem com o capítulo; `longevidade` é categoria (`curta`), sem número.
- **17:17** · `tsc` limpo; `validate` vermelho na primeira vez só por citações de linha deslocadas (`artes-grid-mesa.ts`/`artes-grid.ts`); `Auditoria_Tecnica.md` e `L-simulacao-simultaneo.md` estavam limpos, então rodei o `reapontar.mjs` (13 citações). `validate` exit 0, `build` exit 0, links e textos conferidos no `dist/`. Commit `8b16b56`.
- **17:19** · relato `91-executora.md` escrito.
- **17:21** · ajustes da Revisora da 90 pedidos pelo Arquiteto. (a) fronteira do Resistir reescrita: a dor física não é teste de Virtude, fica Vigor + Convicção (Artes, Estabilizar); e veneno, doença e ambiente são Vigor + Resistência (`acoes-resistir.md:34,68,112`), e não Vigor + Convicção como o conserto sugerido no veredito dizia. A palavra "tortura" não entrou: espera a PERGUNTA 2. (b) `90-executora.md`: 17% vira 8%, com a nota de correção no lugar. (c) a nota da Revisora era sobre o gatilho 3 da MANUTENÇÃO ("ser obrigado a ficar parado"), e não sobre a provocação, que já dizia "é obrigado ao teste": devolvido o "ser obrigado a". (d) arredondamento: o livro não é uniforme (ver relato). `validate` e `build` exit 0, 23 capítulos no `dist/regras`.
